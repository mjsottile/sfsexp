This is a backup of two different implementations of the parser from
the transition period between v0.1.x and 0.2.x.  Not very useful for now,
and only here in case the 0.2.x version failed completely.  Since 0.2.x is
very stable and works great, this will probably disappear some day.
